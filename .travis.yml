language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t themindset/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true themindset/docker-react yarn test
